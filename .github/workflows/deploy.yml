name: Deploy to GitHub Pages

# Publishes dist/ (built with the live Pages URL) to GitHub Pages.
# Enable once under Settings → Pages → "Build and deployment" → GitHub Actions.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - uses: actions/configure-pages@v6
        id: pages
      - name: Build dist for the live Pages URL
        run: BASE_URL="${{ steps.pages.outputs.base_url }}" npm run build
      - name: Verify built manifest
        run: npm run validate:dist
      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
